const path = require('path');const filePath = path.resolve(__dirname, "keytar.node");try { global.process.dlopen(module, filePath); } catch(exception) { throw new Error('Cannot open ' + filePath + ': ' + exception); };